# Compatibility install. Prefer: pip install .
jsonschema>=4.23,<5
PyYAML>=6.0.2,<7

# Optional: expose the generator as MCP tools (tools/mcp_server.py).
# Install with:  pip install "mcp[cli]>=1.2"
# mcp[cli]>=1.2

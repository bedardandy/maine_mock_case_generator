# Contributing

Use Python 3.12 or newer. Install with `pip install -e ".[dev]"`, run
`python -m pytest`, and run `mmcg ecosystem-smoke` before opening a pull request.

All fixtures must remain fictional, deterministic, and free of real personal data.
Changes to schemas, adapters, catalog locks, or repository capabilities require
tests and a changelog entry.
